#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(){
	int N=0,k=99,i,j,cmp;
	char R[10]="0";
	char S[10]="0";	
	scanf("%d",&N);
	for(i=0;i<N;i++){							
		scanf("%s",R);
		scanf("%s",S);					
		switch(R[10]){
			case'spock':
				scanf("%s",S);
				cmp=strcmp(R,S);				
				if(cpm<0){
					
				}
				
		}
		if(cmp==0){
			printf("empate");			
		}
			
	}
	return 0;	
}
